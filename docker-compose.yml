services:
  db:
    image: postgres:16
    restart: always
    ports:
      - '5432:5432'
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: password
      POSTGRES_DB: fab_league
    volumes:
      - fab_league_pgdata:/var/lib/postgresql/data

volumes:
  fab_league_pgdata:
